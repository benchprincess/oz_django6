# Django 라이브강의 덕배강사님

# 1일차

## .으로 시작하는 파일
- . 으로 이름이 시작하는 파일이나 디렉터리는 "숨겨진 파일" OR "숨겨진 디렉터리"
- 숨겨진 것들은 그냥 'ls' 로는 조회가 안되고, 'ls-al'로 조회할 수 있다.

## PEP?
- Python Enhancement Proposal
- PEP 는 규칙이 아니라 "제안"
- 진짜로 cpython을 개발하는 파이썬 커미터들 포함, 파이썬 커뮤니티에서
    "어떻게 앞으로 개선할 것인가"를 놓고 서로 제안하고, 승인하기도 하고, Decline 하기도 함
- 승인된 PEP는 실제로 구현이 되서, 다음 파이썬 버전에 포함된다.
- e.g.) PEP 255는 "제너레이터"입니다. ㅔ데 255에 의해서 제너레이터가 도입되었습니다.
- 공식문서(python officail document, PEP, django documentation)를 가까이 하도록

##
hi = 'abc'
hi2 = "abc"
둘다 str

## Black
- code formatter, 코드 스타일을 맞춰준다.

## git commit 하기 전에 (중요)
- 자기가 뭘 커밋하는지 꼭, 커밋전에 한번 더 확인하기
- 지우지 않은 print() 가 있는지 확인
    - print는 IO를 수반하기 때문에, (터미널에 출력해야 되기 때문) 비싼 연산
    - 의도하지 않은 print가 남아있지 않도록합시다.

## poetry 종속성 관리
- pooetry add를 실행할 때 '-b' 옵션을 주면, 종속성을 dev dependency 로 추가합니다.

